import { FilterMenuDirective } from './filter-menu.directive';

describe('FilterMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterMenuDirective();
    expect(directive).toBeTruthy();
  });
});
