import { HambergerMenuDirective } from './hamberger-menu.directive';

describe('HambergerMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new HambergerMenuDirective();
    expect(directive).toBeTruthy();
  });
});
